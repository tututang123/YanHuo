# 2026-07-07 日记 Daily

## 随手记录

- 切换到工作 HAB 项目，处理服务端同学反馈的活动页和 Bonus 展示问题。

## 今日询问

| 时间 | 问题 | 类型 | 背景/目标 | 状态 |
| --- | --- | --- | --- | --- |
| 09:51 | HAB 服务端反馈：Free bet / Bonus 展示字段错误，Weekly Reload / First Recharge 请求协议需要补 activity_conf_id | 项目 | 修复前端协议和页面取值，保证活动规则页按服务端返回配置精确获取 | 已解决 |

## 今日解决

| 问题 | 解决方式 | 产出/链接 | 后续 |
| --- | --- | --- | --- |
| HAB 活动规则页请求和 Bonus 展示问题 | 更新 `api.proto`，重新生成 protobuf；`FirstRechargeInfoReq` 传 `activity_conf_id`；`WeeklyReloadInfoReq` 传 `weekly_reload_activity_type` 和 `activity_conf_id`；top-up 路径传 prepare-recharge 返回的活动配置 ID；命令入口继续透传 weeklyReloadActivityType；Bonus/Free bet 展示改为使用实际字段，不再写死 wagering requirements；纠正 `FreeBetItem` 弹窗 Got from 取 `freeBetCoupon.gotFrom`；补查并修正 `BonusPage` 展开详情 Got from 取 `BonusItem.gotFrom`；`Wagering requirements` 改为 `${wager_multiplier}x(${当前货币符号}${wager_requirement_amount})` 格式 | `E:\flutter\hab_client` 工作区改动；定向 `dart analyze` 无 error，仅剩项目已有 info 级 lint | 等你检查资源/页面效果后决定是否提交 |

## 工作

- HAB 前端：修复服务端反馈的活动协议参数和优惠券展示字段。

## 生活

- 

## 学习

- 

## 未解决问题

- HAB 全项目 `flutter analyze` 仍有大量历史 info 级 lint，当前任务未处理。

## 明天

- 
